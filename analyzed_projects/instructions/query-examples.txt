# Query Examples for ScriptBee Data Model

These examples demonstrate how to query the in-memory graph via the `execute_code` MCP tool.
The `graph_data` dict is pre-initialized - just use it directly in your code.

NOTE on metric names: trait names like `anomaly.testing.BugMagnet`, overview names
like `authorship`, and relation kinds like `cochange.component-component` are shown
as concrete strings here for clarity, but the canonical list lives in code. Call
`list_metrics()` once per session to see the current catalog — new metrics show up
there automatically. If a name in this file no longer appears in `list_metrics()`,
the metric was renamed; treat the example as a pattern template.

---

## 1. Project overview: name and commit count

```python
git_project = graph_data["git"]
num_commits = len(git_project.git_commit_registry.all)
project_name = git_project.name

print(f"Project '{project_name}' has {num_commits} commits.")
```

## 2. Top 5 most modified files

```python
from collections import Counter

git_project = graph_data["git"]
file_counter = Counter()

for file in git_project.file_registry.all:
    fname = file.last_existing_name()
    file_counter[fname] = len(file.changes)

top_5_files = file_counter.most_common(5)
print("The 5 most modified files (with number of modifications):")
for fname, count in top_5_files:
    print(f"  {fname}: {count} modifications")
```

## 3. Which user contributed to the most Jira issues?

```python
from collections import defaultdict

git_project = graph_data["git"]
user_issue_count = defaultdict(set)

for account in git_project.account_registry.all:
    for commit in account.commits:
        for issue in commit.issues:
            user_issue_count[account].add(issue)

most_contributing_user = max(user_issue_count.items(), key=lambda x: len(x[1]), default=None)

if most_contributing_user:
    user, issues = most_contributing_user
    print(f"User {user.git_id} contributed to {len(issues)} Jira issues (the most).")
else:
    print("No user found with associated issues.")
```

## 4. Top 5 "bug magnet" files (cross-project query)

```python
from collections import Counter

jira_project = graph_data["jira"]
bug_file_counter = Counter()

for issue in jira_project.issue_registry.all:
    for issue_type in issue.issue_types:
        if issue_type.name.lower() == "bug":
            for commit in issue.git_commits:
                for change in commit.changes:
                    bug_file_counter[change.file.relative_path()] += 1

top_5_bug_magnets = bug_file_counter.most_common(5)

print("Top 5 bug magnet files:")
for fname, count in top_5_bug_magnets:
    print(f"  {fname}: {count} occurrences in bug-related commits")
```

## 5. Pull requests with linked JIRA issues

```python
github_project = graph_data["github"]

for pr in github_project.pull_request_registry.all:
    if pr.issues:
        issue_keys = ", ".join(i.key for i in pr.issues)
        print(f"PR #{pr.number} '{pr.title[:50]}' -> Issues: {issue_keys}")
```

## 6. BugMagnet files (using the enrichment layer)

```python
# `find_files_with_trait` is pre-injected into /execute scope.
files = find_files_with_trait("anomaly.testing.BugMagnet")
print(f"{len(files)} BugMagnet files")
for fid in files[:10]:
    print(f"  {fid}")
```

## 7. Co-change neighbours of a file

```python
neighbours = cochange_neighbors("src/server.py", window="lifetime", limit=10)
for fid, strength in neighbours:
    print(f"{strength:>5.0f}  {fid}")
```

## 8. Authorship table (project + per-folder rollup)

```python
authorship = overview_as_dict("authorship")
for entity, cells in authorship["rows"].items():
    bf1 = cells.get("bus_factor_1_files", {}).get("lifetime_value")
    total = cells.get("total_authors", {}).get("lifetime_value")
    print(f"{entity:30s} authors={total} bus_factor_1_files={bf1}")
```

## 9. Filter classifiers (e.g. weekend bugfixes)

```python
e = graph_data["enrichments"]
weekend_bugfixes = [
    t for t in e.tags_by_entity.values()
    if t.entity_kind == "commit"
    and t.classifiers.get("message.nature") == "bugfix"
    and t.classifiers.get("weekday") in ("sat", "sun")
]
print(f"{len(weekend_bugfixes)} weekend bugfix commits")
```

## 10a. Components and their file counts

```python
e = graph_data["enrichments"]
for comp in e.components:
    print(f"{comp.name} ({comp.path_prefix}): {len(comp.file_paths)} files")
```

## 10b. Issue type impact (intent_impact overview)

```python
table = overview_as_dict("intent_impact")
for entity, cells in table["rows"].items():
    issues = cells.get("issue_count", {}).get("lifetime_value")
    churn = cells.get("linked_churn", {}).get("lifetime_value")
    print(f"{entity:20s} issues={issues} linked_churn={churn}")
```

## 10c. PR -> reviewer (proxy fallback) edges

```python
e = graph_data["enrichments"]
rf = e.relation_file("pr.reviewer", "lifetime")
if rf:
    for r in rf.relations[:10]:
        proxy = "PROXY" if r.extras.get("proxy") else "explicit"
        print(f"PR #{r.source_id} -> {r.target_id} ({r.strength}, {proxy})")
```

## 10d. Component-to-component co-change (cross-area churn coupling)

```python
e = graph_data["enrichments"]
rf = e.relation_file("cochange.component-component", "lifetime")
if rf:
    for r in rf.relations[:10]:
        print(f"{r.source_id} <-> {r.target_id}: {r.strength}")
```

## 9b. Files over 1000 LOC (Lizard B1)

```python
# `list_file_metrics` is the new MCP tool; here's the equivalent inside execute_code.
metrics = (graph_data.get("metrics") or {}).get("lizard") or []
big = sorted([m for m in metrics if m.sum_nloc >= 1000], key=lambda m: -m.sum_nloc)
for m in big[:10]:
    print(f"{m.sum_nloc:>6} NLOC  ccn_max={m.max_ccn:<4} fns={m.function_count:<4}  {m.file_path}")
```

## 9c. DynamicBlob anomaly files

```python
files = find_files_with_trait("anomaly.cohesion.size.DynamicBlob")
print(f"{len(files)} DynamicBlob files (high-LOC + high-churn)")
for fid in files[:10]:
    print(f"  {fid}")
```

## 9d. Files most coupled by method calls (JaFax / B2)

```python
# Top 10 file pairs by inter-file method-call count.
ef = graph_data["enrichments"].relation_file("calls.file-file", "lifetime")
if ef is None or not ef.relations:
    print("No JaFax data ingested.")
else:
    for r in ef.relations[:10]:
        print(f"  {int(r.strength):>5} calls  {r.source_id} -> {r.target_id}")
```

## 9e. ZoneCrossroad (timezone-anomaly) files

```python
files = find_files_with_trait("anomaly.cohesion.ZoneCrossroad")
print(f"{len(files)} files with commits across multiple time zones")
for fid in files[:10]:
    print(f"  {fid}")
```

## 9g. Top file pairs by external duplication (DuDe / B3)

```python
# `duplication.file-file.external` strength = total duplicated lines (sum of
# block_length across all duplicated blocks between the two files). Pairs in
# the same immediate parent directory go to `duplication.file-file.sibling`
# instead.
ef = graph_data["enrichments"].relation_file("duplication.file-file.external", "lifetime")
if ef is None or not ef.relations:
    print("No DuDe data ingested.")
else:
    for r in ef.relations[:10]:
        blocks = r.extras.get("block_count", "?")
        print(f"  {int(r.strength):>5} lines / {blocks} blocks  {r.source_id} <-> {r.target_id}")
```

## 9h. Files with the most internal duplication (DuDe / B3)

```python
# `duplication.file-file.internal-summary` is a per-file scalar emitted as a
# self-loop edge (source_id == target_id == file_path). Strength = duplicated
# lines inside the file.
ef = graph_data["enrichments"].relation_file("duplication.file-file.internal-summary", "lifetime")
if ef is None or not ef.relations:
    print("No DuDe internal data ingested.")
else:
    for r in ef.relations[:10]:
        print(f"  {int(r.strength):>4} duplicated lines  {r.source_id}")
```

## 9f. FeatureEncapsulationOverview snapshot

```python
table = overview_as_dict("feature_encapsulation")
for row in table["rows"][:10]:
    cells = row["cells"]
    print(
        f'{row["entity_id"]:<24} '
        f'files={cells["file_count"]["lifetime_value"]:<5} '
        f'kloc={cells["source_loc_kloc"]["lifetime_value"]} '
        f'wide%={cells["wide_commit_pct"]["lifetime_value"]} '
        f'high_impact_tasks={cells["high_impact_task_count"]["lifetime_value"]}'
    )
```

## 9i. Top files by Insider code-smell count (B4)

```python
# Insider records are stashed at graph_data["quality_issues"]; each issue carries
# `occurrence_count` (the raw `value` field) which is a count of rule firings,
# NOT a 1-5 severity ordinal.
qi = graph_data.get("quality_issues")
if qi is None or not qi.issues:
    print("No Insider data ingested.")
else:
    from collections import defaultdict
    per_file = defaultdict(int)
    for issue in qi.issues:
        per_file[issue.file_path] += issue.occurrence_count
    for fid, total in sorted(per_file.items(), key=lambda kv: -kv[1])[:10]:
        print(f"  {total:>5} smells  {fid}")
```

## 9j. Files carrying a specific Insider code-smell trait

```python
# Trait names are data-driven: anomaly.codesmell.{Category}.{Rule} where
# whitespace is stripped from both segments. Run get_quality_issues_summary()
# (MCP) or check graph_data["quality_issues"].issues to discover what fired.
files = find_files_with_trait("anomaly.codesmell.Traceability.CatchTopLevelException")
print(f"{len(files)} files flagged with Catch Top-Level Exception")
for fid in files[:10]:
    print(f"  {fid}")
```

## 9k. CodeQualityOverview snapshot (B4)

```python
table = overview_as_dict("code_quality")
for row in table["rows"][:10]:
    cells = row["cells"]
    print(
        f'{row["entity_id"]:<24} '
        f'smells={cells["total_smells"]["lifetime_value"]:<5} '
        f'rules={cells["distinct_rules"]["lifetime_value"]:<3} '
        f'files={cells["distinct_files"]["lifetime_value"]:<4} '
        f'top={cells["top_rule"]["lifetime_value"]} '
        f'({cells["top_rule_count"]["lifetime_value"]})'
    )
```

## 10. Commits per author (top 10)

```python
from collections import Counter

git_project = graph_data["git"]
author_counts = Counter()

for commit in git_project.git_commit_registry.all:
    if commit.author:
        author_counts[commit.author.git_id.name] += 1

print("Top 10 contributors by commit count:")
for name, count in author_counts.most_common(10):
    print(f"  {name}: {count} commits")
```

---

## Usage Notes

- `graph_data` is pre-initialized. Do not re-declare it.
- Do NOT mutate the graph data - it is shared in-memory across executions.
- Use print() for all output. The result is captured stdout.
- Keep output concise: summarize, aggregate, or limit to top N.
- Standard library modules (collections, datetime, etc.) are available.
- Access registries via `.all` for iteration, `.get_by_id()` for lookup.
